package generators;

public enum OperatorType {
  NOT,
  UNARY_MINUS,
  LEN,
  ORD,
  CHR,
  BITWISE_NOT,
  MULT,
  DIVIDE,
  MOD,
  PLUS,
  BINARY_MINUS,
  GREATER_THAN,
  GREATER_THAN_EQ,
  SMALLER_THAN,
  SMALLER_THAN_EQ,
  EQ,
  NOT_EQ,
  BITWISE_AND,
  BITWISE_OR,
  BITWISE_XOR,
  AND,
  OR
}
